
  # Shortify URL Shortener UI

  This is a code bundle for Shortify URL Shortener UI. The original project is available at https://www.figma.com/design/4gRntQh4zjsaymdMkbPLxR/Shortify-URL-Shortener-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  